//given a string, remove any duplicate letters from it
//letters removed will be added to another string called duplicates

var originalString = [ "ttis", "ttis", "a", "string", "ttotally"]
var duplicates = []

function removeDuplicates() {
    for(i=0; i < originalString.length; i++){
        if (originalString[i]) {
            console.log("The duplicate is this")
        }
    }
}


removeDuplicates()